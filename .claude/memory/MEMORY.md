# flow — project memory

Repo-local memory index. One line per memory; see `CLAUDE.md` for the read-at-start convention.

- [Fork strategy](flow-fork-strategy.md) — how flow customizes Excalidraw: submodule fork, ride upstream, keep diff lean/additive, API-where-clean
- [Sloppiness → global setting](flow-sloppiness-global.md) — DONE (2026-07-07): app-wide sloppiness pref in File▸Preferences, flow-level not fork
- [Desktop menu bar](flow-desktop-menu-bar.md) — File/View/Help bar + Preferences/About, Radix, flow-level, no fork edits (shipped 2026-07-07)
- [Rename wimp → flow](rename-wimp-to-flow.md) — 2026-07-07 project rename: branding, storage keys (no migration), fork branch, directory, memory moved repo-local
- [Left-panel accordion](left-panel-accordion.md) — IN PROGRESS: Illustrator-style dockable panels replacing the island; Phase 0–1 done (selection-style core + docking engine), P2+ pending
- [Pending follow-ups](pending-followups.md) — deferred tasks; currently: purge local git recovery refs from the 2026-07-08 .claude history scrub
- [Vertical toolbar](vertical-toolbar.md) — flow-native left tool rail (float/close/configurable), replaces Excalidraw's island; shipped 2026-07-08
- [Quick actions bar](quick-actions-bar.md) — flow-native horizontal top bar (arrange/group/align/toggles/undo-redo/tools), right of the menu; adds the arrow-binding lock (2nd fork edit); shipped 2026-07-08
- [Bottom bar](bottom-bar.md) — flow-native horizontal bottom-left bar (grid/zen/zoom/canvas-bg/search); zero fork; shipped 2026-07-08
- [Search sub-panel](search-panel.md) — flow-native canvas search in the controls dock (fork export #2 `getSearchMatches`); native sidebar retired; bottom bar + Ctrl/F auto-open it; shipped 2026-07-09
- [Arrowhead size](arrowhead-size.md) — per-end arrow head size slider (Stroke panel), size = strokeWidth × factor; fork SCHEMA edit (start/endArrowheadSize on linear elements); shipped 2026-07-09
- [Transform panel](transform-panel.md) — top-of-dock sub-panel for numeric W/H/X/Y/rotation; shipped 2026-07-09; reuses Excalidraw resize (additive export). **Radius moved to the Stroke panel and padding to the Text panel on 2026-08-05**, both now multi-selection
- [Laser color](laser-color.md) — global laser-pointer trail color+opacity; 2nd appState fork field (mirrors bindingMode); shipped 2026-07-09, **moved out of the Color panel into File ▸ Preferences on 2026-08-07** (retiring `ColorRow`'s `onWrite` hatch)
- [Selection mode](selection-mode.md) — Preferences `Select: [marquee touch] [marquee enclose]`; 3rd appState fork field (`getElementsWithinSelection` touch/enclose); vendor rebuild+types gotcha; shipped 2026-07-09
- [Grid size preference](grid-size-preference.md) — global flow.gridSize (5–100, default 20) in File ▸ Preferences; zero fork (native appState field); shipped 2026-07-09
- [View-menu toggles](view-menu-toggles.md) — View menu: live checkboxes for Grid/Snap/Arrow-Binding/Tool-Lock/Zen; useViewToggles reactive bridge; zero fork; shipped 2026-07-09
- [Flow-owned global appState](flow-global-appstate.md) — `FLOW_GLOBAL_APP_STATE_KEYS` stripped on doc open so a saved scene can't clobber flow prefs; ADD NEW GLOBAL PREFS TO THAT LIST **and to `flowSeedAppState`**, which `initialData` and File ▸ New share (2026-08-08: New's `resetScene` wiped the seed, and the roughness mismatch made the onChange normalizer clone mid-drag → boxes drew 0×0); shipped 2026-08-04
- [Color swatches](color-swatches.md) — SUPERSEDED 2026-08-11: the Swatches *panel* merged into the Color panel (see [[color-system]]); palette-store design below is still current
- [Color system](color-system.md) — 2026-08-11: one Color panel (part chooser + HSV picker + merged palette section) replacing four scattered surfaces, plus a rail color control; color derived from the selection via `useSelectionStyle`, never stored; `useColorDraft`'s seen/emitted re-seed rule guards hue (wrong twice before correct); `needsRevival` stroke-width coupling shared by 4 write paths; eyedropper fork export is 2 re-export lines with handle-scoped cancellation; confirmed the 2 `text-panel.spec.ts` e2e failures are pre-existing on `main`. **Second pass, same day** (`feat/color-picker-refinement`): part-chooser artwork moved from CSS `::after` tricks to `PartArt.tsx`'s widest-first concentric SVG strokes (reversing the layer order is a silent regression), quartet went 2×2, palette grid got a drag-to-delete trash tile (`aria-disabled`, never `disabled` — Chrome drops mouse events on disabled controls, which would silently break drop), `ColorPreview` removed; the rail vertical-fit e2e test's first draft silently measured the wrong (2-part) case because Escape-after-text-edit clears selection (same root cause as the known `text-panel.spec.ts` failures) — fixed to force a real re-select and assert 3 radios before measuring, so a future regression fails loudly instead of quietly passing on the short case. **Third pass**: the chooser became a fixed-size object — all three parts always render, unavailable ones dimmed not removed — so the panel's top row is two equal-height sections and the saturation box (aspect-ratio dropped in the panel only) scales horizontally alone
- [Drawing defaults](drawing-defaults.md) — square corners, 0–10px stroke slider, tight selection chrome (`SELECTION_SPACING` fork edit); 2px stroke was already upstream; shipped 2026-08-04. **The 0-width slider has cost three fork edits so far** (fill-maths floor; `stroke:"transparent"` not `"none"`, else curved arrows crash; `restore.ts` `??` not `||`, else paste/file-open silently rewrite 0 → 2) — audit falsy coercions on `strokeWidth` in vendor code
- [Optional-prop undo](flow-optional-prop-undo.md) — flow-added element props must be written via `newElementWith` or history misses them; undo can never return one to never-set (vendor `applyDelta` skips `undefined`)
- [Scrub numeric inputs](scrub-numeric-inputs.md) — drag-to-scrub on every panel number field + native spin buttons (grip retired); transient/EVENTUALLY undo batching; the live-echo commit bug; shipped 2026-08-05
- [Style memory](style-memory.md) — four session-scoped per-category style buckets (shape/linear/text/freedraw); adopt-on-select + currentItem* drift capture + load-on-tool-change; only contended keys are bucketed, padding stays resident; fork adds currentItemCornerRadius/Padding + a `_newElementBase` field-drop trap; shipped 2026-08-06
- [Tool override](tool-override.md) — hold Cmd/Ctrl for a temporary selection tool (restores tool + selection); tool lock forced permanently on and its three UI surfaces removed; found regression (forcing the lock also disabled vendor's auto-select-on-draw) fixed via a 2-site fork edit decoupling the two; shipped 2026-08-07 including a final-review wave and a corrective pass on top of it (search-box `Q` swallow, release-time style-memory reload redone to route through `useStyleMemory`'s own load path), unit 640/640 + e2e 113/113 green
- [Excalidraw upgrade](excalidraw-upgrade.md) — 2026-08-11: fork moved 382 commits to upstream master (branch now `flow-next`, old `flow` stale); replay-don't-merge across the monorepo restructure; fork diff shrank 33→22 files; four load-bearing new fork edits; the `excalidrawAPI`→`onExcalidrawAPI` rename that silently nulled flow's api handle; the tool-lock/auto-select family (4 sites); kill stray vite servers before trusting e2e
